# How to Run

* Install latest fullsend CLI
* Install latest Openshell
* Replace variables on `.env`
* Run `fullsend run rfe-creator --fullsend-dir .fullsend --target-repo . --env-file .env`


## State

I'm now at the point where it kind of runs, but it does not write an output JSON file as expected.
